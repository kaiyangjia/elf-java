package com.jiakaiyang.elf.java.statistics.condition;

/**
 * 由多个基本条件组成的复核条件
 */
public class CompositionCondition<T> extends ConditionImpl<T>{
    @Override
    public boolean accept(T data) {
        return super.accept(data);
    }
}
